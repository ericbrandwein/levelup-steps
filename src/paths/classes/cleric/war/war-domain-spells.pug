extends ../domain-spells-template.pug

block spells
    if level === 3
        | magic weapon, spiritual weapon.
    if level === 5
        | crusader's mantle, spirit guardians.
    if level === 7
        | freedom of movement, stoneskin.
    if level === 9
        | flame strike, hold monster.

