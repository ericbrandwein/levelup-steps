extends ../domain-spells-template.pug

block spells
    if level === 3
        | augury, suggestion.
    if level === 5
        | nondetection, speak with dead.
    if level === 7
        | arcane eye, confusion.
    if level === 9
        | legend lore, scrying.

